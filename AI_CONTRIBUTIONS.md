# AI Contribution Registry

This project intentionally uses multiple AI systems. This file makes that work
visible without treating any model as the sole author.

## Count

- **Distinct contributing model entries:** 6
- **Providers represented:** 3
- **Contribution records:** 17


Counts include only models with a concrete contribution and evidence. Gemini
contributed on 2026-07-18 (AI-003, Review R-001) and is counted above.

## Model registry

| ID | Model / product | Provider | First contribution | Attribution note |
| --- | --- | --- | --- | --- |
| AI-001 | Claude (exact model unknown) | Anthropic | 2026-07-17 | Existing git history includes merged branch `claude/nervous-mcnulty-402e56`; the exact model/version was not recorded. |
| AI-002 | Codex (GPT-5) | OpenAI | 2026-07-18 | Created the sanctuary free-roam brief and multi-AI collaboration context. |
| AI-003 | Gemini 3.5 Flash (High) | Google | 2026-07-18 | Reviewed the Sanctuary Free-Roam Redesign plan and appended Review R-001. |
| AI-004 | Claude Fable 5 (`claude-fable-5`) | Anthropic | 2026-07-18 | Reviewed the Sanctuary Free-Roam Redesign plan and appended Reviews R-002 and R-003. Correction: first registered as "Claude Opus 4.8", which was wrong — the session's environment reports `claude-fable-5`. Corrected same-day by the same session (C-007), per the no-guessed-versions rule. |
| AI-005 | Claude Opus 4.8 (`claude-opus-4-8`) | Anthropic | 2026-07-18 | Built the sanctuary exterior (BaseScene grounds) and interior (VaultScene), porting the two `iso designs/` HTML prototypes onto the repo's existing tile/decor engine. Model ID reported by the session environment as `claude-opus-4-8`. |
| AI-006 | Claude Sonnet 5 (`claude-sonnet-5`) | Anthropic | 2026-07-18 | Generalized the Base roster from wyvern-only to a multi-species companion system with a bond/interaction stat. Model ID reported by the session environment as `claude-sonnet-5`. |

## Contribution log

Append one row for each material work session. Keep old rows unchanged.

| Record | Date | Model ID | Contribution | Main files / evidence | Verification |
| --- | --- | --- | --- | --- | --- |
| C-001 | 2026-07-17 | AI-001 | Earlier Claude-assisted development represented by the merged Claude branch. | Git merge `c1a7c1f`; branch name preserved in history | Existing merge history only; exact session details unavailable |
| C-002 | 2026-07-18 | AI-002 | Planned sanctuary free roam, zoom/follow/survey camera behavior, interaction architecture, isometric readability work, and multi-AI handoff conventions. | `docs/SANCTUARY_FREE_ROAM_PLAN.md`, `AI_CONTEXT.md`, `AGENTS.md`, `GEMINI.md`, README/roadmap context | Documentation links and repository consistency checked |
| C-003 | 2026-07-18 | AI-002 | Rebased the detached documentation work onto the latest local `main` lineage so other agents and worktrees can discover it safely. Updated the shared commands for the newer Vite workflow. | Branch `codex/multi-ai-sanctuary-docs`; documentation commit `4f82280` | Clean cherry-pick onto `de0b3d8`; `git diff --check`, syntax, and atlas validation passed; full check stopped because `vitest` is not installed in this worktree |
| C-004 | 2026-07-18 | AI-002 | Added a structured multi-model sanctuary review workflow with focused questions, a copyable prompt, append-only response template, and human-owned decision log. Routed Codex/agents, Claude, and Gemini entry files to it. | `docs/SANCTUARY_FREE_ROAM_PLAN.md`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md` | Documentation diff and heading/link checks |
| C-005 | 2026-07-18 | AI-003 | Reviewed the Sanctuary Free-Roam Redesign plan and appended Review R-001 to the multi-model review workspace. | `docs/SANCTUARY_FREE_ROAM_PLAN.md` | Markdown structure and document verification passed; no code modified. |
| C-006 | 2026-07-18 | AI-004 | Appended Review R-002: five code-grounded Milestone 1 failure seams (pointerdown gate vs. drag-pan, missing camera bounds + bias padding, fit-sized backdrop, zoom-baked label fonts, stale fit zoom), five engine recommendations including reusing the already-registered eight-direction animation keys, and twelve art-direction findings covering the terrain/atlas zoom-quality conflict, directional art priority against the 68 MiB atlas budget, and flight shadow altitude cues. Recorded one disagreement with R-001 on collision scope. | `docs/SANCTUARY_FREE_ROAM_PLAN.md` | Source read across 14 files; `npm run check` and browser pass **not run** — no code modified, and all behavioral statements in R-002 are marked as predictions to confirm in Milestone 1. |
| C-007 | 2026-07-18 | AI-004 | Appended Review R-003 (verification/accessibility focus, plan question 8): pure-data contract test proposals in the repo's wyvernAtlas pattern, reduced-motion and keyboard-focus acceptance items, pointer-loss and letterbox checks, and a numeric perf target. Ran the check gate and recorded observed results. Corrected AI-004's own registration from "Claude Opus 4.8" to `claude-fable-5` in the registry and in R-002's heading, and fixed the stale "Gemini is not counted" note to match the existing AI-003 entry. | `docs/SANCTUARY_FREE_ROAM_PLAN.md`, `AI_CONTRIBUTIONS.md` | Observed on a Linux copy of the worktree (mounted `node_modules` is macOS-native; worktree not modified): `check:syntax` pass (35 modules), `validate:atlas` 0 errors / 1 pre-existing warning, `vitest` 9/9 pass, `vite build` pass. Browser smoke test **not run**. |
| C-008 | 2026-07-18 | AI-005 | Built the base/sanctuary as two separate, in-world-connected scenes from the `iso designs/` prototypes: exterior Mossy Monolith island (BaseScene) and interior Emberkeep Dragonvault (VaultScene), reached via a clickable barred gate / daylight glow rather than a view toggle (per owner instruction to keep layers as separate scenes and leave MissionScene untouched). Added 8 sanctuary biome palettes, moss/springwater/grout top-textures, the monolith-niche tile overlay, 11 ported interior prop drawers with an enlarged decor box, `data/sanctuary.js` hand-authored layouts + resident spots, `systems/sanctuaryRender.js`, `ui/roostPanel.js`, per-view backdrops, and per-species resident textures. Roster animals appear as residents in both views. | `src/scenes/BaseScene.js`, `src/scenes/VaultScene.js`, `src/data/sanctuary.js`, `src/systems/sanctuaryRender.js`, `src/ui/roostPanel.js`, `src/systems/{tileArt,decorArt,textureBake}.js`, `src/data/biomes.js`, `src/config.js`, `src/scenes/PreloadScene.js`, `CLAUDE.md` | `node --check` pass on all `src/**/*.js`; browser smoke test pass — both scenes render, gate/glow/button transitions work, recruit adds a resident, Mission still renders, zero console errors. `npm run check` (vitest/atlas/build gate) **not run**. |
| C-009 | 2026-07-18 | AI-005 | Built the world atlas as the fourth separate scene (`AtlasScene`) and the game's mission select, porting a reference world-map design onto the existing tile/decor engine: `atlasWorld.js` region-blob generator returning the same `{tiles,cols,rows}` contract as `terrain.js`, `data/atlas.js` (7 regions, 12 seeded POIs, region blobs + atoll ring), `ui/atlasPanel.js` overlay, 7 atlas biome palettes (badlands/taiga/snow/darkwood/jungle/ocean/atoll) with aliased top-textures, 9 POI marker + 3 atlas biome prop drawers, and inline camera pan/zoom. Wired Base/Vault → Atlas → Mission with each POI's `seed` threaded through `MissionScene` to `buildTerrain()`, so every destination is a distinct island. Second pass reworked the camera to frame the island rather than the grid (fit-to-land, cursor-anchored zoom, ocean fade at the grid edge, collapsible panel that re-fits edge-to-edge) and re-authored the region blobs so the on-screen compass layout matches the reference map (taiga N, badlands NW, desert W, grass centre, snow NE, darkwood E, jungle SE, atoll S). | `src/scenes/AtlasScene.js`, `src/systems/atlasWorld.js`, `src/data/atlas.js`, `src/ui/atlasPanel.js` (new); `src/data/biomes.js`, `src/systems/{tileArt,decorArt}.js`, `src/config.js`, `src/main.js`, `src/scenes/{BaseScene,VaultScene,MissionScene}.js`, `src/ui/{roostPanel.js,ui.css}`, `CLAUDE.md`, `ROADMAP.md`; commit `d0738c9`, branch `feat/world-atlas` | `node --check` pass on all `src/**/*.js`; Node-harness validation of the pure generator (ASCII render, determinism, per-POI region-consistency, and a 200-seed regression confirming no atlas/sanctuary biome leaks into missions); browser end-to-end drive pass — atlas frames the whole island, pan/zoom + cursor-anchored zoom + tooltip + region filter + POI card work, two different POIs produce visibly different mission islands (231/256 cells differ), collapse re-fits edge-to-edge, return to Base leaves no stale DOM, zero console errors. `npm run check` (vitest/atlas/build gate) **not run**. |
| C-010 | 2026-07-18 | AI-006 | Applied the repo's biome-registry pattern (`data/biomes.js` → `decorArt.js`) to sanctuary creatures: new `data/species.js` registry (wyvern + griffon, id/name/emoji/hpBase/hpPerLevel); generalized `systems/roster.js` from a wyvern-only model to a per-species one (`getWyvern`→`getAnimal`, species-aware `gainXp`, new `raiseBond`/`recruitAnimal`); redesigned `BaseScene.buildOverlay()` from one global Train/Build pair into a per-animal card (avatar, xp bar, new bond bar, scoped Train/Feed buttons) plus a species-driven recruit row generated from `Object.values(SPECIES)`, mirroring the existing `WYVERN_ORDERS`-driven button-row pattern in `MissionScene.buildHud()`. Documented the new "add a species" seam in `CLAUDE.md` and narrowed the corresponding `ROADMAP.md` Phase 3 item. | `src/data/species.js` (new); `src/systems/roster.js`, `src/scenes/BaseScene.js`, `src/scenes/MissionScene.js`, `src/ui/ui.css`, `CLAUDE.md`, `ROADMAP.md` | `node --check` pass on all `src/**/*.js`; `npm run check` full gate pass (`check:syntax` 35 modules, `validate:atlas` 0 errors/1 pre-existing warning, `vitest` 9/9, `vite build`); browser end-to-end drive pass — Feed fills a distinct pink bond bar independent of xp, Train levels xp on only the clicked card, recruiting a Griffon adds an independently-tracked card (110 hp vs. the wyvern's 100, 0 bond/xp) with no cross-talk between roster entries, Launch Mission still boots the mission unaffected, zero console errors. |
| C-011 | 2026-07-18 | AI-005 | Reviewed the Game UI Redesign plan through a "Floating UI" style brief and appended Review R-001 to its multi-model review workspace. Argued for adopting floating *structure* while keeping the dark palette (white cards would blow out over the baked terrain art), and supplied a dark-mode elevation recipe since the existing black-on-near-black shadow carries almost no luminance delta. Answered the plan's four open questions, proposing a shared floating nav island instead of the offered bottom-right-slot / panel-footer options for question 1. Recorded four source-grounded findings: B-1, `#ui-overlay` is `inset: 0` on the window while `main.js` letterboxes the canvas via `Scale.FIT`, so every overlay gutter is measured from the wrong rectangle (worked example: `.btn-view` lands 21px below the canvas at 1440x900); B-2, `.vault-action-dock`'s `min-width: 520px` overrides its `min()` width and overlaps both side panels between 1081px and roughly 1200px, uncovered by the existing `max-width: 1080px` media query; B-3, `.panel-pill` and `.btn-view` inherit `border-radius: 9px` and are not pills; B-4, `.atlas-panel` and `.vault-profile-panel` span full height and read as docked rails rather than floating cards. Confirmed from source that relocating the Vault diagnostics is zero-risk because `updateVaultDiagnostics` resolves `[data-vault-diagnostic]` against `#ui-overlay` rather than the profile panel. Proposed reordering the milestones so the coordinate-space and float-layer fixes precede the token migration. | `docs/UI_REDESIGN_PLAN.md`, `AI_CONTRIBUTIONS.md`; findings read from `src/ui/ui.css`, `src/ui/{roostPanel,vaultPanel,atlasPanel}.js`, `src/scenes/MissionScene.js`, `src/main.js`, `src/config.js` | No code modified; documentation-only change. `npm run check` and browser pass **not run**. The B-1 letterbox arithmetic and B-2 overlap arithmetic are computed from the CSS and the Phaser scale config, not observed in a browser, and are marked in R-001 as requiring visual confirmation. The `updateVaultDiagnostics` finding is read directly from `src/ui/vaultPanel.js:162-169`. |
| C-012 | 2026-07-18 | AI-003 | Reviewed the Game UI Redesign plan and appended Review R-002 to its multi-model review workspace. Supported the Floating UI layout and proposed concrete implementation designs for letterbox scale alignment (B-1), focused keyboard event isolation, prefers-reduced-motion compatibility, and Google Fonts typography. Outlined a browser/accessibility verification route. | `docs/UI_REDESIGN_PLAN.md` | No code modified; documentation-only change. `npm run check` and browser pass **not run**. |
| C-013 | 2026-07-18 | AI-003 | Executed the UI Redesign Plan Milestones 2-5: refactored Roost, Vault, Atlas, and Mission overlays to Floating UI card/island anatomy. Configured Vitest to run DOM tests under jsdom by installing jsdom and creating vitest.config.js. Fixed template literal parsing errors in roostPanel.js, vaultPanel.js, and atlasPanel.js. | `src/ui/{roostPanel.js,vaultPanel.js,atlasPanel.js}`, `src/scenes/{VaultScene.js,MissionScene.js}`, `src/ui/uiKit.test.js`, `vitest.config.js`, `src/ui/ui.css`, `package.json`, `task.md`, `walkthrough.md` | `npm run check` full gate pass (Vitest passes 11/11 tests, build succeeds) |
| C-014 | 2026-07-18 | AI-003 | Changed the UI theme colors to a Dark Luxe Black & Gold palette, refactoring the previous slate and purple styles in src/ui/ui.css to semantic variables and clean metallic/bronze gradients. | `src/ui/ui.css` | `npm run check` full gate pass (11/11 Vitest tests, syntax check, atlas validation, and production build pass). |
| C-015 | 2026-07-18 | AI-002 | Revised the Vault overlay to let players independently hide and restore the roster, profile, animation actions, and travel navigation. Reflowed the profile and action dock so they reserve space above the navigation instead of overlapping it. | `src/ui/vaultPanel.js`, `src/scenes/VaultScene.js`, `src/ui/ui.css` | `npm run check` (syntax, atlas validation with one existing size warning, 11 tests, production build) |
| C-016 | 2026-07-18 | AI-002 | Removed the duplicate castle icon from the Roost “Enter the Vault” navigation label, retaining the shared navigation icon. | src/scenes/BaseScene.js | npm test (11 tests passed) |
| C-017 | 2026-07-18 | AI-003 | Closed the Sanctuary Free-Roam Redesign plan as requested, marking its status as Closed. | docs/SANCTUARY_FREE_ROAM_PLAN.md | No code modified; documentation-only change. |

## How another model adds itself

1. If the exact model/version is not already in the registry, add one row with
   the next `AI-###` ID and increment **Distinct contributing model entries**.
2. If this adds a new provider, increment **Providers represented**.
3. Add one contribution row with the next `C-###` record and increment
   **Contribution records**.
4. Name the files or commit that prove the contribution and report the checks
   actually run. Use `not run` when appropriate.
5. Never add a placeholder model, infer an exact version, or rewrite another
   model's record.

Copyable contribution row:

```md
| C-### | YYYY-MM-DD | AI-### | What changed and why. | Files, commit, or PR | Checks run / not run |
```

Copyable model row:

```md
| AI-### | Product (exact model/version) | Provider | YYYY-MM-DD | First contribution and attribution evidence. |
```
