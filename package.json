{
  "name": "wyvern-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:atlas": "node scripts/validate-wyvern-atlases.mjs",
    "check:syntax": "node scripts/check-syntax.mjs",
    "check": "npm run check:syntax && npm run validate:atlas && npm test && npm run build"
  },
  "dependencies": {
    "phaser": "3.80.1"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
