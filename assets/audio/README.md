# Audio

Music and SFX (.mp3/.ogg). Load in PreloadScene with this.load.audio(...) and
play via this.sound.play('key'). Remember browsers need a user gesture before
audio starts — the Launch Mission button click satisfies that.
